# Frontend Shared Layer

Cross-feature reusable modules live here:
- `ui/` design-system primitives
- `lib/` cross-cutting utilities
- `types/` shared type definitions
- `config/` shared constants and runtime config

Feature-specific logic should remain under `src/frontend/features/<feature>`.
